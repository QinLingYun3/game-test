## Why

项目处于先行版阶段，得分和排行榜记录可能随时因版本更新被重置。需要用户明确同意后才可开始游戏。

## What Changes

- 首页输入房号下方新增一个复选框 + 同意文案
- 只有打勾后才能点击「开房」「加入房间」「开始游戏（单人）」
- 未勾选时按钮置灰，点击提示"必须同意才能开始游戏"
- 勾选状态记录在 localStorage，下次自动勾选

## Impact

- Affected code: `src/App.jsx`, `src/i18n.js`, `src/styles.css`
- 纯前端改动，不涉及后端
