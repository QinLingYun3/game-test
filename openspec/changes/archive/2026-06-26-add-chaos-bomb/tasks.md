## 1. Server

- [x] 1.1 实现 `useChaosBomb` — 复用 `useSmokeBomb` 逻辑
- [x] 1.2 服务端清理逻辑：6s 过期 + 队列提升

## 2. Frontend

- [x] 2.1 前端检测 `chaos` 类型 `activeItems`
- [x] 2.2 混沌效果：tile 图标 CSS 动画轮换
- [x] 2.3 目标玩家头像下方显示标记
- [x] 2.4 效果到期自动恢复

## 3. i18n

- [x] 3.1 新增 `item.chaos` / `item.chaosDesc` 文案
